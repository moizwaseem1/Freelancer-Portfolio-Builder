document.addEventListener('DOMContentLoaded', () => {
    const landingContainer = document.querySelector('.landing-container');
    const h1 = landingContainer.querySelector('h1');
    const p = landingContainer.querySelector('p');
    const btn = landingContainer.querySelector('.get-started-btn');

    // This is optional but can be used for more complex animations.
    // For this design, CSS animations are handling the work.
    // We'll leave this here as a placeholder for future dynamic features.
});


